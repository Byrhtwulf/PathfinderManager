'use strict';

describe('myApp.CombatManager module', function() {

  beforeEach(module('myApp.CombatManager'));

  describe('CombatManager controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});